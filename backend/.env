# No API key required — fully offline, rule-based generation

# spaCy model (optional — tool works without it, falls back to blank English)
SPACY_MODEL=en_core_web_sm

# Tuning
CHUNK_SIZE_WORDS=1500
MAX_RETRIES=3
DEDUP_THRESHOLD=0.85
